package piconot.external
import piconot.internal._

package object semantics {
//  def eval(expr: RuleExp): OurSurroundings = expr match {
//    case OurSur(x, y, z, t) ⇒ OurSurroundings(x, y, z, t)
//  }
}